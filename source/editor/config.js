import logger from 'editor/common/logging.js'

const config = {
  idCanvas : 'canvas',
  logLevel : logger.SILLY,

  // classes for html elements
  classCanvas           : 'b3-canvas',
  classTree             : 'tree',
  classSelectedTree     : 'selected',
  classSelectedBlock    : 'selected',
  classLayer            : 'layer',
  classLayerBlocks      : 'blocks',
  classLayerConnections : 'connections',
  classLayerComments    : 'comments',
}
export default config
